task zipTask(type: Zip) {
    archiveFileName = "exercise4-textfiles.zip"
    destinationDirectory = file("$buildDir/dist")

    from ("$projectDir/files") {
        include '*.txt'
    }
}

task copyTask(type: Copy) {
    from zipTask
    into "${projectDir}/dist"
}

