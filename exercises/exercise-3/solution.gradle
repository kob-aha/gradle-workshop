task hello {
    doFirst {
        print "Hello "
    }
}

task fromGradle {

    // What should we add here to make sure this task will be executed after hello task? (hint: it's not "dependsOn")
    
    doFirst {
        println "From Gradle !!!"
    }
}

task helloFromGradle (dependsOn: [fromGradle, hello]) {}